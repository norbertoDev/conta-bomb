import React from "react";
import { HeaderContainer } from "./styles";

const Header = () => {
    return (
    <HeaderContainer>
        esto es el header
    </HeaderContainer>
    );
}

export default Header;